import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.multident.webmobile',
  appName: 'multident-webmobile',
  webDir: 'dist'
};

export default config;
